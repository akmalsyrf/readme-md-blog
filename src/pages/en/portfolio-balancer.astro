---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getTranslations, getLocaleFromPath } from '../../utils/i18n';
import PortfolioBalancerContent from '../../components/others/PortfolioBalancer/PortfolioBalancerContent.astro';

const currentPath = Astro.url.pathname;
const locale = getLocaleFromPath(currentPath);
const t = getTranslations(locale);
---

<BaseLayout title={t.portfolioBalancer.title} description={t.portfolioBalancer.description}>
  <div class="container mx-auto px-4 py-8">
    <div class="mx-auto max-w-5xl">
      <div class="mb-6">
        <h1 class="mb-2 text-3xl font-bold text-gray-900 dark:text-gray-100">
          {t.portfolioBalancer.title}
        </h1>
        <p class="text-gray-600 dark:text-gray-400">{t.portfolioBalancer.description}</p>
      </div>

      <PortfolioBalancerContent locale={locale} t={t} />
    </div>
  </div>
</BaseLayout>
